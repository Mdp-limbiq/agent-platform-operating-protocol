name: domain-runtime-agent
version: 0.1.0
contract_version: "1.0"

agent:
  type: custom
  entrypoint: your_package.agent.YourDomainAgent

evolvable_layers:
  - prompts
  - skills
  - memory

reload_strategy: hot

